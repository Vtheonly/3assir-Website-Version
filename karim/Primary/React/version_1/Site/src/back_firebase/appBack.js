// Import the writeData function
const { writeData, getData } = require("./firebase.js");

// Data to be written to Firestore
const data = {
  g: "88888",
  dd: "wehfo",
  ehfeh: "ehoehfoq",
  negga: "dd",
};

// Write data to Firestore
// writeData(data);


getData



console.log(x)